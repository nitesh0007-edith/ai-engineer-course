---
term: Pre-commit hook
definition: A script that git runs automatically right before it creates a commit, and that can block the commit if it fails. The pre-commit framework manages a project's hooks (typically a linter, a type checker, sometimes tests) from one .pre-commit-config.yaml file, so every contributor runs the same checks before committing, not just whoever remembers to.
---
