---
term: Kernel trick
definition: Computing inner products corresponding to a richer feature mapping without explicitly constructing every mapped coordinate. It allows an SVM to form nonlinear boundaries while keeping a linear separator in the implicit feature space.
---
