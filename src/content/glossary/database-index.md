---
term: Database index
definition: An additional data structure that helps a database find selected rows without scanning every row. It speeds up matching reads but consumes storage and adds work to inserts and updates, so indexes should follow real query patterns rather than being added to every column.
---
