---
term: Decorator
definition: A function that wraps another function to add behaviour around it — retries, timing, caching, logging — without changing the wrapped function's own code. Applied with the `@decorator` syntax on the line above a `def`. The standard way to attach cross-cutting concerns to the many API calls in an AI system.
---
