---
term: Type hints
definition: >-
  Optional annotations that declare the expected types of variables, arguments
  and return values (e.g. `def f(x: int) -> str`). Python does not enforce them
  at runtime, but tools like mypy, your editor, and libraries like Pydantic read
  them to catch bugs before the code ever runs.
---
