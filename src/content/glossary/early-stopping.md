---
term: Early stopping
definition: A predefined rule that stops training when a validation measure has not improved for a chosen number of checks. It uses validation data to select duration and must not repeatedly expose the final test set.
---
