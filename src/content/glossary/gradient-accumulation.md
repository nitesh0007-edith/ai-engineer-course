---
term: Gradient accumulation
definition: A technique that adds gradients from several micro-batches before one optimiser step. It creates a larger effective batch without holding all examples in memory at once; mean losses normally need scaling by the accumulation count.
---
