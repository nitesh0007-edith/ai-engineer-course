---
term: Virtual environment
definition: An isolated set of installed Python packages, separate from your system Python and from any other project's environment, so two projects that need different versions of the same library never conflict. uv creates and manages one per project automatically.
---
