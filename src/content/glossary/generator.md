---
term: Generator
definition: A function that uses `yield` to produce values one at a time, on demand, instead of building a whole list in memory. The consumer pulls the next value when it wants it; the generator pauses in between. It is how you stream LLM tokens as they arrive and how you process datasets too large to fit in memory.
---
