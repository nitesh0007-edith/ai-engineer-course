---
term: Module
definition: In PyTorch, an nn.Module is a reusable model building block. It groups calculations and registered parameters so the framework can find those parameters, switch the block between training and evaluation behaviour, and save or load its state.
---
