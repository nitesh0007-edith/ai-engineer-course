---
layer: 0
order: 1
title: "Python foundations for reliable AI applications"
slug: "python-for-ai-engineering"
subtitle: "Build dependable AI application code with clear data, streaming, cleanup, errors, logs, and project structure."
depth: build
difficulty: beginner
readingTime: 70
prerequisites:
  - how-to-use-this-course
skillsGained:
  - "Store text in variables and lists"
  - "Read a small Python function"
  - "Clean a group of document strings"
  - "Recognise and fix a shared-list default bug"
  - "Use type hints and dataclasses to describe AI application data"
  - "Choose a comprehension or generator for data processing and streaming"
  - "Add shared behaviour with decorators and cleanup with context managers"
  - "Handle expected errors, log safely, and organise a small AI project"
objectives:
  - "Explain what variables, lists, loops and functions do in a small Python program"
  - "Trace a document-cleaning function from its input to its output"
  - "Write a function that removes blank text and surrounding spaces"
  - "Identify and fix the mutable-default list mistake"
  - "Write type hints, a dataclass, a comprehension, and a generator"
  - "Explain decorators, context managers, exceptions, and logging in an AI application"
concepts:
  - variable
  - list
  - function
  - type-hints
  - dataclass
  - generator
  - decorator
  - context-manager
  - comprehension
  - exception-handling
  - logging
  - project-layout
diagrams:
  - PythonDocumentPipeline
  - ReliablePythonWorkbench
interactives:
  - PyRunner
  - QuizCard
  - FlashcardDeck
  - LessonChecklist
codeExamples: 6
exercises:
  - prompt: "Clean a list of three support messages by removing surrounding spaces and ignoring empty messages."
    difficulty: core
    solution: "```python\ndef clean_messages(messages):\n    cleaned = []\n    for message in messages:\n        tidy = message.strip()\n        if tidy:\n            cleaned.append(tidy)\n    return cleaned\n```"
resources:
  - title: "Python tutorial — an informal introduction"
    url: "https://docs.python.org/3/tutorial/introduction.html"
    type: docs
    note: "The official introduction to values, strings and lists used in this lesson."
  - title: "Python tutorial — defining functions"
    url: "https://docs.python.org/3/tutorial/controlflow.html#defining-functions"
    type: docs
    note: "The official reference for function parameters, return values and default arguments."
  - title: "Python tutorial — data structures"
    url: "https://docs.python.org/3/tutorial/datastructures.html"
    type: docs
    note: "A useful next reference once lists and loops feel familiar."
status: published
updated: 2026-07-27
---

import PyRunner from "../../../components/interactive/PyRunner.tsx";
import QuizCard from "../../../components/interactive/QuizCard.tsx";
import FlashcardDeck from "../../../components/interactive/FlashcardDeck.tsx";
import LessonChecklist from "../../../components/interactive/LessonChecklist.tsx";
import PythonDocumentPipeline from "../../../components/diagrams/PythonDocumentPipeline.astro";
import ReliablePythonWorkbench from "../../../components/diagrams/ReliablePythonWorkbench.astro";
import LessonIdentity from "../../../components/lesson/LessonIdentity.astro";
import MainIdea from "../../../components/lesson/MainIdea.astro";
import AnalogyCard from "../../../components/lesson/AnalogyCard.astro";
import CodeNotebook from "../../../components/lesson/CodeNotebook.astro";
import ExpectedOutput from "../../../components/lesson/ExpectedOutput.astro";
import LineExplanation from "../../../components/lesson/LineExplanation.astro";
import RealWorldExample from "../../../components/lesson/RealWorldExample.astro";
import PracticeActivity from "../../../components/lesson/PracticeActivity.astro";
import RevisionQuestions from "../../../components/lesson/RevisionQuestions.astro";
import SketchnoteSummary from "../../../components/lesson/SketchnoteSummary.astro";

Python is the glue in many AI applications.

The AI model may produce the answer. Python handles the work around it. Python receives the user's text, cleans it, sends it to the model, checks the reply, and decides what happens next.

You do not need to know all of Python before you can build something useful. You need a small set of ideas and a clear picture of how data moves through them.

<LessonIdentity
  module="AI engineering foundations"
  lesson="1 · Python foundations for AI applications"
  difficulty="Beginner"
  duration="32 minutes"
  prerequisites={[
    "You can use a web browser",
    "You have completed How to use this course",
    "No previous Python is required",
  ]}
  skills={frontmatter.skillsGained}
/>

## Why this matters

AI applications receive untidy data. A support message may contain extra spaces. A document list may contain blank entries. A model should not have to guess which text is useful.

Python lets you turn that messy input into a predictable shape before another part of the system uses it.

<MainIdea>
  A Python program is a sequence of small instructions. You give values clear
  names, group related values, repeat work with a loop, and place reusable steps
  inside a function. Reliable AI code comes from making each small step easy to
  inspect.
</MainIdea>

<Objectives data={frontmatter} />

## Start with four small building blocks

A <Term id="variable">variable</Term> is a name that points to a value.

```python
message = "  Where is my order?  "
```

The name is `message`. The value is the text inside the quotation marks.

A <Term id="list">list</Term> keeps several values in order. Python lists use square brackets:

```python
messages = ["  Refund please  ", "   ", "Where is my order?"]
```

A **loop** repeats an instruction. A `for` loop can visit each item in a list, one at a time.

A <Term id="function">function</Term> is a named, reusable group of instructions. A function can receive an input and return an output.

<AnalogyCard
  title="A function is a small kitchen station"
  limitation="A real cook can notice unexpected situations and improvise. A Python function follows only the instructions you wrote."
>
  Imagine a kitchen station that washes vegetables. A basket arrives as the input. The station follows the same steps for every vegetable. A clean basket leaves as the output.

A Python function works in the same pattern: input → named steps → output.

</AnalogyCard>

## See the whole idea

<Figure
  id="00-01.1"
  label="MESSY TEXT BECOMES USEFUL INPUT"
  caption="— three raw strings enter the clean function. Surrounding spaces and one blank string are removed, leaving two useful strings."
>
  <PythonDocumentPipeline />
</Figure>

The diagram contains three stages:

1. **Input:** a list holds three pieces of text.
2. **Process:** the function removes surrounding spaces and ignores text that becomes empty.
3. **Output:** a new list holds only useful text.

The original input stays separate from the result. This makes the function easier to reason about.

## Follow the steps

### Step 1: receive the input

The function receives a list through a **parameter**. A parameter is the name a function uses for an input.

```python
def clean_messages(messages):
```

Here, `messages` is the parameter.

### Step 2: make an empty output list

```python
cleaned = []
```

The function needs somewhere to collect its results. It starts with an empty list.

### Step 3: visit each message

```python
for message in messages:
```

On each pass through the loop, `message` points to one value from the input list.

### Step 4: clean and check the text

```python
tidy = message.strip()
if tidy:
```

The string method `.strip()` removes spaces from the beginning and end.

An `if` statement runs its indented code only when a condition is true. An empty string counts as false, so `if tidy:` rejects blank text.

### Step 5: return the result

```python
cleaned.append(tidy)
return cleaned
```

`.append()` adds one value to a list. `return` sends the finished list back to the code that called the function.

## Worked example

Trace this input by hand:

```python
["  Refund please  ", "   ", "Where is my order?"]
```

| Loop pass | Current value          | After `.strip()`  | Keep it? | Output so far                             |
| --------- | ---------------------- | ----------------- | -------- | ----------------------------------------- |
| 1         | `"  Refund please  "`  | `"Refund please"` | Yes      | `["Refund please"]`                       |
| 2         | `"   "`                | `""`              | No       | `["Refund please"]`                       |
| 3         | `"Where is my order?"` | unchanged         | Yes      | `["Refund please", "Where is my order?"]` |

Nothing mysterious happens between input and output. The function applies the same two checks to every item.

## Run the code

<CodeNotebook title="Clean a list of support messages">

```python
def clean_messages(messages):
    cleaned = []

    for message in messages:
        tidy = message.strip()
        if tidy:
            cleaned.append(tidy)

    return cleaned


raw_messages = ["  Refund please  ", "   ", "Where is my order?"]
print(clean_messages(raw_messages))
```

</CodeNotebook>

<ExpectedOutput>

```text
['Refund please', 'Where is my order?']
```

</ExpectedOutput>

<LineExplanation
  items={[
    {
      line: "def clean_messages(messages):",
      explanation: "Creates a reusable function and names its input messages.",
    },
    {
      line: "cleaned = []",
      explanation: "Creates a fresh output list each time the function runs.",
    },
    {
      line: "for message in messages:",
      explanation: "Visits every input string, one at a time.",
    },
    {
      line: "tidy = message.strip()",
      explanation:
        "Removes spaces from both ends without changing spaces inside the sentence.",
    },
    {
      line: "if tidy:",
      explanation: "Runs the next line only when some text remains.",
    },
    {
      line: "return cleaned",
      explanation: "Sends the completed list back to the caller.",
    },
  ]}
/>

Now run it in your browser. Change one message. Add an empty string. Predict the output before selecting **Run**.

<PyRunner
  client:idle
  id="00-01.A"
  label="CLEAN THE SUPPORT QUEUE"
  code={`def clean_messages(messages):
    cleaned = []

    for message in messages:
        tidy = message.strip()
        if tidy:
            cleaned.append(tidy)

    return cleaned

raw_messages = [" Refund please ", " ", "Where is my order?"]
print(clean_messages(raw_messages))

# Try adding " Password reset " to raw_messages.`}

/>

## A bug worth remembering

The original lesson opened with a Python trap that has appeared in real software:

```python
def add_message(message, batch=[]):
    batch.append(message)
    return batch

print(add_message("first"))
print(add_message("second"))
```

The surprising output is:

```text
['first']
['first', 'second']
```

Python creates the default list once, when it defines the function. Later calls reuse that same list.

Use `None` as the default, then create a new list inside:

```python
def add_message(message, batch=None):
    if batch is None:
        batch = []
    batch.append(message)
    return batch
```

This matters in an AI system because shared state can mix data from separate requests.

## Part 2 — Python patterns for reliable AI applications

The first part gives you the language basics. These patterns make a small AI
application easier to change, test, and debug.

<Figure id="00-01.2" label="THE PYTHON AROUND A MODEL" caption="— dependable AI systems need clear data shapes, careful streaming, shared reliability behaviour, cleanup, visible failures, and a predictable project layout. The model call is only one step.">
  <ReliablePythonWorkbench />
</Figure>

### Type hints say what data is expected

<Term id="type-hints">Type hints</Term> are labels for expected data shapes.

~~~python
def clean_documents(documents: list[str]) -> list[str]:
    return [document.strip() for document in documents if document.strip()]
~~~

The function expects a list of strings and returns a list of strings. Python
does not enforce this at runtime, but editors, type checkers, and validation
libraries can spot mismatches early. Hints do not replace validation for
untrusted user or API data.

### Dataclasses group related values

A <Term id="dataclass">dataclass</Term> is a lightweight way to name related
configuration values.

<CodeNotebook title="Keep chunking configuration together">

~~~python
from dataclasses import dataclass, field

@dataclass
class ChunkConfig:
    size: int = 300
    overlap: int = 40
    sources: list[str] = field(default_factory=list)

config = ChunkConfig(sources=["handbook.md"])
print(config)
~~~

</CodeNotebook>

<ExpectedOutput>

~~~text
ChunkConfig(size=300, overlap=40, sources=['handbook.md'])
~~~

</ExpectedOutput>

<LineExplanation items={[{ line: "@dataclass", explanation: "Creates useful initialisation and display behaviour from declared fields." }, { line: "size: int = 300", explanation: "Names an integer field and its default value." }, { line: "field(default_factory=list)", explanation: "Creates a fresh list for every object, avoiding the shared mutable-default bug." }]} />

### Comprehensions are short, visible transformations

A <Term id="comprehension">comprehension</Term> builds a collection in one
clear expression.

~~~python
titles = [document.title() for document in documents if document.strip()]
~~~

Read it left to right: produce a title for each non-blank document. Use a
comprehension for one obvious map or filter. Use a normal loop when steps need
names, error handling, logging, or more than one decision.

### Generators stream one value at a time

A <Term id="generator">generator</Term> uses yield to pause and produce each
value only when the next consumer asks for it.

~~~python
def non_blank_lines(path):
    with open(path, encoding="utf-8") as file:
        for line in file:
            tidy = line.strip()
            if tidy:
                yield tidy
~~~

Lists are eager: they build every value now. Generators are lazy: they make
the next value when needed. This suits large files, document pipelines, and
streaming model tokens. Generators are single-use; make a new generator to
iterate again.

### Decorators add shared behaviour around a function

A <Term id="decorator">decorator</Term> wraps a function to add timing, retries,
rate limiting, or logging.

~~~python
from functools import wraps
from time import perf_counter

def timed(function):
    @wraps(function)
    def wrapper(*args, **kwargs):
        start = perf_counter()
        result = function(*args, **kwargs)
        print("seconds:", round(perf_counter() - start, 3))
        return result
    return wrapper
~~~

Keep decorators small. Too many stacked wrappers hide control flow and make
debugging difficult. Retry only errors you expect and can safely repeat.

### Context managers guarantee cleanup

A <Term id="context-manager">context manager</Term> is used with with. It
guarantees cleanup even if code inside fails.

~~~python
with open("ticket.txt", encoding="utf-8") as file:
    ticket = file.read()
# file is closed here, even if read() raised an exception
~~~

Use context managers for files, database transactions, network clients, locks,
and timing scopes.

### Handle expected errors and log without secrets

<Term id="exception-handling">Exception handling</Term> catches a specific
expected error. Do not use a bare except, which catches everything and can hide
programming mistakes.

~~~python
import logging

logger = logging.getLogger(__name__)

def parse_limit(raw_limit: str) -> int:
    try:
        limit = int(raw_limit)
    except ValueError as error:
        raise ValueError("limit must be a whole number") from error

    logger.info("parsed document limit: %d", limit)
    return limit
~~~

<Term id="logging">Logging</Term> records operational events. Prefer it to
print in application code. Use placeholders so expensive values are not
formatted unless needed. Never log API keys, passwords, full private prompts,
or personal data unless a narrowly justified policy permits it.

### Give the project a home for each concern

A <Term id="project-layout">project layout</Term> prevents one giant file from
becoming the whole application.

~~~text
support_assistant/
├── pyproject.toml
├── README.md
├── src/support_assistant/
│   ├── config.py
│   ├── clients.py
│   ├── pipeline.py
│   └── logging.py
└── tests/
~~~

Keep configuration, external clients, domain pipeline logic, and tests
separate. A src layout also makes accidental imports from the working directory
less likely.

## Real-world AI engineering use

<RealWorldExample
  title="Cleaning support tickets before classification"
  steps={[
    "Receive ticket text",
    "Clean and validate it",
    "Send useful text to the model",
    "Store the checked result",
  ]}
>
  Imagine a model that sorts support tickets into billing, delivery, or account problems. Sending blank and badly shaped input wastes work and makes failures harder to diagnose.

A small Python function cleans each ticket first. A later validation step can reject missing fields. The model then receives a predictable input.

</RealWorldExample>

The cleaning function is not artificial intelligence. It is software engineering around the model. AI engineering needs both.

## Common mistakes

<CommonMistakes
  items={[
    {
      mistake: "Changing the input list while looping over it.",
      fix: "Build a separate output list. Removing items from the list you are visiting can make Python skip the next item.",
    },
    {
      mistake: "Forgetting to return the result.",
      fix: "Add return cleaned after the loop. Without return, Python gives the caller None.",
    },
    {
      mistake: "Putting return inside the loop.",
      fix: "Align return with the for line. If it is inside the loop, the function stops after the first message.",
    },
    {
      mistake: "Using an empty list as a default parameter.",
      fix: "Use None and create a fresh list inside the function so separate calls do not share data.",
    },
  ]}
/>

## Knowledge check

Choose an answer. Feedback appears immediately and explains why.

<QuizCard
  client:load
  questions={[
    {
      question: "What is a variable?",
      options: [
        "A name that points to a value",
        "A type of loop",
        "An error message",
      ],
      answer: 0,
      explanation:
        "A variable gives a value a name so the program can use that value later.",
    },
    {
      question: "What does message.strip() change?",
      options: [
        "All spaces everywhere",
        "Spaces at the beginning and end",
        "The order of the words",
      ],
      answer: 1,
      explanation:
        ".strip() removes surrounding whitespace. It keeps spaces between words.",
    },
    {
      question: "Why is return placed after the loop?",
      options: [
        "To return only the first item",
        "To wait until every item has been checked",
        "Because loops cannot contain return",
      ],
      answer: 1,
      explanation:
        "Returning inside the loop would stop the function on its first pass. Returning afterwards sends back the complete result.",
    },
    {
      question: "What is wrong with def add_message(message, batch=[])?",
      options: [
        "Lists cannot hold text",
        "The same default list can be reused across calls",
        "Functions cannot have two parameters",
      ],
      answer: 1,
      explanation:
        "Python creates that default list once. Use None and create a new list inside when no list was supplied.",
    },
    {
      question: "What is a type hint useful for?",
      options: [
        "Describing expected data to people and tools",
        "Forcing Python to validate every value at runtime",
        "Making a generator reusable",
      ],
      answer: 0,
      explanation:
        "Hints describe expected types. Editors and type checkers can use them, but runtime validation still needs explicit code or a validation library.",
    },
    {
      question: "When is a generator especially useful?",
      options: [
        "When processing or streaming many values one at a time",
        "When every value must be stored in a list first",
        "When a function needs a shared default list",
      ],
      answer: 0,
      explanation:
        "Generators produce the next value on demand, which can reduce memory use for large files and streaming responses.",
    },
    {
      question: "What does a context manager help guarantee?",
      options: [
        "Cleanup after a file or client is used",
        "That every model response is correct",
        "That exceptions never happen",
      ],
      answer: 0,
      explanation:
        "A with block runs cleanup when it finishes, including when an exception interrupts the code inside.",
    },
    {
      question: "Why avoid a bare except clause?",
      options: [
        "It can hide unexpected bugs and important failures",
        "Python cannot catch ValueError",
        "Logging cannot be used with exceptions",
      ],
      answer: 0,
      explanation:
        "Catch only errors you expect and can handle. Unknown failures should remain visible with useful context.",
    },
  ]}
/>

## Practice activity

<PracticeActivity
  title="Prepare feedback for a sentiment model"
  time="10–20 minutes"
  goal="Write a function that cleans feedback strings and keeps only useful entries."
  steps={[
    "Start with the code below.",
    "Create an empty results list.",
    "Loop over every feedback string.",
    "Remove surrounding spaces.",
    "Keep the text only when it is not empty.",
    "Return the finished list and print it.",
  ]}
  expected={`["Great service", "Delivery was late"]`}
  challenge="Return each kept message in lowercase by calling .lower() after .strip()."
>

Starter code:

```python
def prepare_feedback(items):
    # Write your code here
    pass


feedback = ["  Great service ", "", "   ", "Delivery was late  "]
print(prepare_feedback(feedback))
```

```python
def prepare_feedback(items):
    prepared = []

    for item in items:
        tidy = item.strip()
        if tidy:
            prepared.append(tidy)

    return prepared


feedback = ["  Great service ", "", "   ", "Delivery was late  "]
print(prepare_feedback(feedback))
# ['Great service', 'Delivery was late']
```

For the optional challenge, change `tidy = item.strip()` to:

```python
tidy = item.strip().lower()
```

</PracticeActivity>

## Key takeaways

<KeyTakeaways
  points={[
    "A variable gives a value a useful name.",
    "A list keeps several values in order.",
    "A loop repeats the same operation for each item.",
    "A function turns named steps into reusable input-to-output behaviour.",
    "Create fresh output lists so separate calls do not accidentally share data.",
    "Type hints and dataclasses make data shape clear before a bug reaches a model call.",
    "Generators stream one item at a time; decorators and context managers add reusable reliability behaviour.",
    "Catch expected errors specifically, log safely, and keep application concerns in separate files.",
  ]}
/>

## Flashcards

<FlashcardDeck
  client:load
  cards={[
    { front: "What is a variable?", back: "A name that points to a value." },
    {
      front: "What brackets create a Python list?",
      back: "Square brackets: [ and ].",
    },
    {
      front: "What does a for loop do?",
      back: "It visits items and repeats a block of instructions.",
    },
    {
      front: "What does a function do?",
      back: "It groups reusable instructions and can turn input into output.",
    },
    {
      front: "What does return do?",
      back: "It ends the function and sends a result back to the caller.",
    },
    {
      front: "Why avoid a list as a default parameter?",
      back: "That one list can be reused across calls and accidentally share data.",
    },
    {
      front: "What do type hints do?",
      back: "They describe expected data types for readers and tools; they are not runtime enforcement by themselves.",
    },
    {
      front: "What is a dataclass useful for?",
      back: "Grouping related typed configuration or data fields without writing repetitive setup code.",
    },
    {
      front: "What is a generator?",
      back: "A single-use producer that yields values on demand instead of building every value now.",
    },
    {
      front: "What does a decorator do?",
      back: "It wraps a function to add shared behaviour such as timing, retries, or logging.",
    },
    {
      front: "Why use with for a file?",
      back: "The context manager closes the file even if work inside the block fails.",
    },
    {
      front: "What must never be put in ordinary logs?",
      back: "Secrets, API keys, passwords, and unnecessary personal or private prompt data.",
    },
  ]}
/>

<RevisionQuestions
  questions={[
    "Explain the path from raw_messages to the returned cleaned list.",
    "What would happen if return cleaned were indented inside the for loop?",
    "Why should an AI application clean and validate text before sending it to a model?",
    "In an interview, how would you explain the mutable-default list bug and its fix?",
    "Compare a comprehension with a generator and choose one for a large document stream.",
    "Explain how a decorator and context manager keep AI application code reliable.",
    "Describe a safe exception-and-logging policy for an API response parser.",
  ]}
/>

## One-page sketchnote summary

<SketchnoteSummary
  title="Python foundations for AI applications"
  terms={[
    "Variable — a name for a value",
    "List — ordered values in square brackets",
    "Loop — repeats work",
    "Function — reusable named instructions",
    "Return — sends a result back",
    "Type hint — expected data shape",
    "Generator — on-demand values",
    "Context manager — guaranteed cleanup",
  ]}
  mistakes={[
    "Do not return from the first loop pass",
    "Do not mutate the list you are visiting",
    "Do not use [] as a default parameter",
    "Do not use bare except",
    "Do not log secrets or personal data",
  ]}
  takeaways={[
    "Name data clearly",
    "Move data through small steps",
    "Build a fresh result",
    "Check output before using a model",
    "Stream large work when possible",
    "Keep shared reliability behaviour small",
  ]}
  questions={[
    "What does .strip() remove?",
    "Why is return after the loop?",
    "How do you make a safe list default?",
    "Why is a generator single-use?",
    "What does with guarantee?",
  ]}
>
  <p slot="main">
    Python connects the parts of an AI application. A dependable function
    receives messy input, applies small visible steps, and returns predictable
    output.
  </p>
  <div slot="diagram">
    <PythonDocumentPipeline compact />
  </div>
  <pre slot="code">
    <code>{`result = []
for item in items:
    tidy = item.strip()
    if tidy:
        result.append(tidy)
return result`}</code>
  </pre>
</SketchnoteSummary>

<LessonChecklist
  client:load
  lessonId="python-for-ai-engineering"
  items={[
    "I can explain variable, list, loop and function.",
    "I traced the worked example.",
    "I ran and changed the Python code.",
    "I completed the knowledge check.",
    "I attempted the practice activity.",
    "I reviewed the flashcards.",
    "I can use type hints and a dataclass.",
    "I can choose a comprehension or generator.",
    "I can explain decorators and context managers.",
    "I can catch expected errors and log safely.",
    "I can describe a small AI project layout.",
  ]}
/>

## Resources

<Resources data={frontmatter} />

<NextUp
  slug="async-python"
  title="Async Python"
  why="You can now follow data through a small function. Next you will learn how Python can pause while an AI model or another service is working, so one slow request does not make every other task wait."
/>
